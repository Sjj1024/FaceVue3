# 人脸识别后端（Vite 代理 /api 时也会读此变量作为 target 的参考，见 vite.config）
# VITE_BACKEND_BASE=http://127.0.0.1:8000

# 队伍服务根地址（「加入队伍」POST /v1/server/mobile_adduser）
# 开发环境：前端请求 /team-api/...，由 Vite 代理到此地址
# 生产构建：需配置此项，前端将直接请求 ${VITE_TEAM_API_BASE}/v1/server/mobile_adduser
VITE_TEAM_API_BASE=http://192.168.31.212:9080
